NPM packages dependency tree calculator.

Supported APIs:
* /packages/:packageName/:packageVersion/dependencies

Installation:
npm install

Start the app:
npm start

Listens on port 8080 by default

Run the tests:
npm test
